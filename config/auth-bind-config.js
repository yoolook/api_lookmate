/* const authCS = require('../Initialize/initialize-secrets'); //Getting values from secrets.
//const authConfigSecret = require('../auth-secrets'); // open this in case secrets are not working, you need to get this file on prod/dev server, in case you want to run this.
console.log("auth-bind-config:authCS:",authCS);
module.exports = authCS;
 */